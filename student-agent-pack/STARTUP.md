## STARTUP COMPLETE

# Startup Checklist

Run this once at the beginning:

```bash
python tools/startup_check.py
```

If checks pass, add this header at the top of this file:

`## STARTUP COMPLETE`

When that header is present, the agent skips startup checks unless you ask to run them again.
